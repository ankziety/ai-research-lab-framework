matplotlib>=3.5.0
numpy>=1.21.0
pytest>=6.0.0

# Multi-agent system dependencies
sentence-transformers>=2.2.0
faiss-cpu>=1.7.0

# LLM API integration (optional but recommended for full functionality)
openai>=1.0.0  # For OpenAI GPT models
anthropic>=0.7.0  # For Claude models

# Optional: For better embedding models and GPU support
# faiss-gpu>=1.7.0  # For GPU-accelerated FAISS (optional)
# torch>=1.11.0  # Required by sentence-transformers